

ep=(predicate)
expr("ep", ep)

state=(predicate)
expr("state", state)

action=(ep)
expr("action", action)

durative=(ep)
expr("durative", durative)

snap=(ep)
expr("snap", snap)

do=(ep)
expr("do", do)

take=(action)
expr("take", take)