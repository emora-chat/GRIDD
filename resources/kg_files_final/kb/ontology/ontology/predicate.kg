

state=(predicate)
expr("state", state)

ep=(predicate)
expr("ep", ep)

durative=(ep)
expr("durative", durative)

snap=(ep)
expr("snap", snap)

action=(ep)
expr("action", action)

do=(ep)
expr("do", do)

take=(action)
expr("take", take)