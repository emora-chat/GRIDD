
greet(emora, user)
-> startc ->
$ Hello how are you ? $
;

time(h/have(user, pet()), now)
request_truth(emora, h)
->
$ Do you have a pet ? $
;

cool(object())
->
$ That's cool. $
;

lame(object())
->
$ That's lame. $
;