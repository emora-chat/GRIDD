r/_request(emora, o/object())
eturn(r, 1)
uturn(o, 0)
-> got_answer ->
_t(_react)
_pr(_low)
$ Gotcha . $
;

r/_request(user, o/object())
uturn(r, 0)
-> unknown_answer_to_user_q ->
_t(_rpresent)
_pr(_low)
$ You know, I can't think of the answer to that right now . $
;
