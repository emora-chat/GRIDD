

g/get_along(user, s/sibling())
request_truth(g)
->
$ Do you get along with your s ? $
;

h/have(user, m/memory())
favorite(m)
with(m, s/sibling())
request_truth(h)
->
$ Do you have a favorite memory with your s ? $
;

why_not_get_along()
->
$ Oh, why don't you guys get along ? $
;