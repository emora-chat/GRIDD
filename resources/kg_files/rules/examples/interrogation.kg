

have(user, x/sibling())
->
g:<g/get_along(user, x)>
request_truth(g)
;

why_not_get_along=(object);

not(g/get_along(user, sibling()))
->
why_not_get_along()
;

get_along(user, sibling())
->
h:<h/have(user, m/memory())
favorite(m)
with(m, sibling())>
request_truth(h)
;





