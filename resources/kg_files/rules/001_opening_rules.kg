imp_trigger(init_ask_name)
-> ask_name_trigger ->
n:<time(h/have(user, n/name()), now)>
request(emora, n)
h{"coldstart": 1}
;

time(have(user, n/name()), now)
r/request(emora, n)
user_aware(r)
-> welcome_convo ->
h/happy(emora)
time(m/meet(emora, user), now)
cause(m, h)
time(hope(emora, feel(user, good)), now)
p:<time(do(user, p/event(object())), today)>
request(emora, p)
;