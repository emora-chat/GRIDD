watchable<entity>
buyable[purchasable]<entity>
movie<watchable,buyable>
avengers[endgame]=movie()

actor<person>
chris_evans[evans,chris evans]=actor()

emora=bot()

like<predicate>
happy<predicate>
watch<predicate>

ela/like(emora,avengers)
reason(ela,like(emora,chris_evans))
test_id[test,id]=time(ela,now)
time(eh/happy(emora),now)
time(ewa/watch(emora,avengers),now)
reason(eh,ewa);

go<predicate>
store<entity>
user=person()
time(go(user,s/store()),past)
time(go(user,s2/store()),future);